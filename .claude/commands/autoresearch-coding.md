---
description: Use the `autoresearch-coding` skill. Require goal, metric command, direction, editable paths, protected paths, budget, and timeout.
---

# autoresearch-coding

Use the `autoresearch-coding` skill. Require goal, metric command, direction, editable paths, protected paths, budget, and timeout.
