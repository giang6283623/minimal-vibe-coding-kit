# Claude init

1. Read `backbone.yml`.
2. If uninitialized, run `/init-vibe` or follow `FIRST_TIME_INIT.md`.
3. For experiments, use `/autoresearch-coding`.
4. For agent-surface changes, use `/security-scan`.
5. For daily improvements, use `/daily-enhance`.
