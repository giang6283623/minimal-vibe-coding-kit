# Autoresearch rules

Use `/autoresearch-coding` for repeated improvement loops. Require goal, metric, direction, editable paths, protected paths, budget, and timeout. Run a baseline before editing and keep only metric-positive or simplification-positive changes.
